# ---------------------------
# MySQL Datasource
# ---------------------------]
spring.datasource.url=${DB_URL:jdbc:mysql://database-1.c5aoagm2w0ql.eu-north-1.rds.amazonaws.com:3306/vppdatainput}
spring.datasource.username=${DB_USER:admin}
spring.datasource.password=${DB_PASS:1313bB12}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---------------------------
# JPA / Hibernate
# ---------------------------
spring.jpa.hibernate.ddl-auto=update
    #validate' or 'none' for prod

spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ---------------------------
# Eureka Client Config
# ---------------------------
eureka.client.service-url.defaultZone=${EUREKA_CLIENT_SERVICES_URL:q}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# ---------------------------
# Logging / Actuator
# ---------------------------
management.endpoints.web.exposure.include=health,info
logging.level.org.springframework=INFO
